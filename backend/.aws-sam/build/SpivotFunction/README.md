# Spivot Backend

FastAPI backend for the Spivot Agentic Operating System.

## Setup

```bash
pip install -r requirements.txt
cp .env.example .env
# Edit .env with your credentials
uvicorn app.main:app --reload
```

## API Docs
Visit `http://localhost:8000/docs` for Swagger UI.
